[project]
name = "world-database"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Ju-Petersen", email = "juliapetersenavi@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "pymysql>=1.2.0",
    "sqlalchemy>=2.0.54",
]

[project.scripts]
world-database = "world_database:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
